<?php

/**
 * Implements hook_theme().
 */
function glenville_card_block_theme($existing, $type, $theme, $path) {
  // Get the module's path via the extension manager service.
  $module_path = \Drupal::service('extension.list.module')->getPath('glenville_card_block');
  $templates_path = $module_path . '/templates/paragraphs';

  return [
    // Parent bundle: glen_grid_block.
    'paragraph__glen_card_block' => [
      'base hook' => 'paragraph',
      'template'  => 'paragraph--glen-card-block',
      'path'      => $templates_path,
    ],
    // Child bundle: glen_card_block_item.
    'paragraph__glen_grid_card_item' => [
      'base hook' => 'paragraph',
      'template'  => 'paragraph--glen-grid-card-item',
      'path'      => $templates_path,
    ],
  ];
}

function glenville_card_block_preprocess_paragraph(array &$vars) {
  $bundle = $vars['paragraph']->bundle();
  if (in_array($bundle, ['glen_card_block', 'glen_card_block_item'], true)) {
    $vars['#attached']['library'][] = 'glenville_card_block/card';
  }
}
